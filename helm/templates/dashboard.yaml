---
{{- range .Values.dashboard }}
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: {{ .name }}
spec:
  resyncPeriod: 30s
  instanceSelector:
    matchLabels:
      app: {{ $.Values.app.name }}
  json: >
  {{ .content | indent 2 }}